[program: {{ app_name }}]
environment=
command={{ venv_dir }}/bin/gunicorn -c gunicorn.conf.py -p gunicorn.pid
directory={{ app_dir }}
user={{ deployer_user }}
autostart=true
autorestart=true
redirect_stderr=true
stdout_logfile=/home/{{ deployer_user }}/logs/{{ app_name }}/supervisor-error.log
